% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dsem.R
\name{build_dsem_objects}
\alias{build_dsem_objects}
\title{Build DSEM data objects using FIMS data structures}
\usage{
build_dsem_objects(
  dsem_settings = NULL,
  data = NULL,
  env_data = NULL,
  n_recdevs = 1L
)
}
\arguments{
\item{dsem_settings}{A list returned by \code{\link[=build_DSEM]{build_DSEM()}}.}

\item{data}{A \code{FIMSFrame} object used to determine model start and end years.}

\item{env_data}{An optional data frame of environmental covariates. If
supplied, it may include a \code{Year} column; missing years are padded to the
FIMS model range.}

\item{n_recdevs}{Number of \verb{recdevs*} columns to prepend.}
}
\value{
A named list with \code{dsem_settings}, \code{dsem_data}, and \code{tmb_inputs}.
}
\description{
Construct DSEM-ready data from a \code{FIMSFrame} object and optional environmental
covariates. This helper mirrors the data-shaping behavior used in Rceattle:
it pads years to the FIMS model range and prepends recruitment-deviation
placeholder columns (\verb{recdevs*}). When a default SEM is generated, equations
are concatenated with \code{"; "} separators.

For first-time users, this function does three jobs:
\enumerate{
\item Builds a clean DSEM data matrix (\code{dsem_data}) aligned to the model years.
\item Builds a complete settings object (\code{dsem_settings}) with validated
\code{family} and SEM text.
\item Builds \code{tmb_inputs}, which are the low-level objects consumed by the
\code{DSEMDistribution} C++ module during fitting.
}
}
\details{
The \code{tmb_inputs} element contains:
\itemize{
\item \code{options}: numeric controls for latent-state parameterization and scaling.
\item \code{RAM} / \code{RAMstart}: sparse structural-equation mapping and starting values.
\item \code{familycode_j}: integer family codes used by the C++ likelihood.
\item \code{y_tj}: observed data matrix for each year (\code{t}) and variable (\code{j}).
\item \code{parameters}: initial values for DSEM parameters and latent states.
}

If the optional \code{dsem} package is installed, FIMS delegates RAM/TMB input
construction to that package for consistency. Otherwise, FIMS uses an
internal parser and mapper that supports the same default workflow.
}
\examples{
dsem <- build_DSEM(family = "normal")

dsem_obj <- build_dsem_objects(
  dsem_settings = dsem,
  data = FIMSFrame(data1),
  env_data = tibble::tibble(Year = 2000:2010, Temp = rnorm(11)),
  n_recdevs = 1
)

names(dsem_obj)
# c("dsem_settings", "dsem_data", "tmb_inputs")
}
